# Application environment (development, production, etc.)
NODE_ENV=production

# Backend server port
PORT=3001

# Allowed origin for CORS (authorized frontend URL)
CORS_ORIGIN=https://app.mydomain.com